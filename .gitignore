# Rust build output
bridge/target/

# Rust
*.rs.bk

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Secrets — never commit anything with a key
*.env
.env
.env.*
secrets/
.secrets/
*.pem
*.key
*token*
*pat*
*credential*

# Tarballs / build artifacts
*.tar.gz
*.tgz
*.zip
/dist/
/build/
/out/

# Local notes
*.log
*.tmp
*.bak

# Magisk module output (if you build it locally)
phosphor-module/
# Runtime exceptions: these MUST ship (fail-closed needs the patterns file)
!runtime/command_risk_patterns.json
!runtime/.env.example
